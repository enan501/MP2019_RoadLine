<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="konkukSW.MP2019.roadline">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".Money.AddMoneyActivity">
        </activity>
        <activity android:name=".Photo.ShowPhotoActivity">
        </activity>
        <activity android:name=".Money.ShowMoneyActivity">
        </activity>
        <activity android:name=".Date.ShowDateActivity">
        </activity>
        <activity android:name=".Date.AddDateActivity">
        </activity>
        <activity android:name=".Date.PickDateActivity">
        </activity>
        <activity android:name=".MainListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>